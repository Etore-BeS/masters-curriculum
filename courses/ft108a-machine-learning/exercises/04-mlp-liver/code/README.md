# Code — Tarefa 3 MLP

Um notebook: `mlp_scratch.ipynb` com NumPy (do zero) + PyTorch + scikit-learn.

1. Na raiz do repo: `uv sync` e `source .venv/bin/activate`
2. Kernel **masters-curriculum**
3. Dados: `../../../data/liver.csv`
4. Partições em `partitions/` → ZIP em `../../entregas/`

Torch is in the root `pyproject.toml` default dependencies (`uv sync`).
